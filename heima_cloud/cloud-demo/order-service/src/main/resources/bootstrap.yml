spring:
  application:
    name: orderservice #服务名,微服务微服务这是一个服务
  profiles:
    active: dev #环境
  cloud:
    nacos:
      server-addr: localhost:80  #nacos 地址
      config:
        file-extension: yaml  #文件后缀名